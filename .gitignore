# =========================================================================
# Public POLNET 2026 release: slides (clean deck) + figures only.
# =========================================================================

# Working paper: the raw SOURCE (paper.md, appendices, journal version,
# reproducibility note) stays out; the shared reading copy and compiled PDF are public.
paper/*
!paper/paper_leitura.md
!paper/Legislative Coordination and Institutional Change (working paper).pdf

# The analysis code and data are released as a full replication package with
# the journal version of the paper, after cleanup. Kept out for now.
scripts/
data/

# Slide files that contain the author's speaker notes stay private.
# Public deck is slides/talk_slides.pdf only.
slides/talk_slides_with_notes.pptx
slides/presenter_handout.pdf

# R session junk
.Rhistory
.RData
.Rproj.user/

# OS junk
.DS_Store
Thumbs.db

# Editor / assistant files (never version these)
CLAUDE.md
PROMPT_INICIAL.md
.claude/
.obsidian/
